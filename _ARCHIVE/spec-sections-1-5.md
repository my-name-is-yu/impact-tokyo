# MimamoAI — 仕様書 セクション1〜5

---

## セクション1: ユーザーストーリー

### ペルソナ A: 田中 恵子（47歳）

- **職業:** 大手メーカーの経理担当（正社員、フルタイム）
- **介護状況:** 実母（78歳）が軽度認知症。同居しており、仕事後の夜間と週末にケアを担う
- **家族構成:** 夫（単身赴任中）、子ども2人（高校生・中学生）
- **困りごと:**
  - 仕事終わりに疲れ果てた状態でケア記録をつける余裕がない
  - ケアマネジャーへの月次報告書作成に毎月2〜3時間かかる
  - 母の体調変化（食事量・睡眠・転倒リスク）を客観的に把握できていない
  - 介護保険の更新手続きや負担軽減制度を調べる時間がない

**典型的な1日の利用シナリオ（恵子さん）**

| 時間 | 行動 | MimamoAI の活用 |
|------|------|-----------------|
| 7:30 | 母の朝食介助 | 食事量・服薬状況をテキストで30秒入力 |
| 8:00 | 出勤 | - |
| 12:00 | 昼休み | 先月分記録をもとに自動生成されたケアマネ報告書ドラフトを確認・修正 |
| 18:30 | 帰宅、夕食介助 | 「今日は食欲なし、トイレ3回、少し混乱あり」と音声入力（P2）→ AIが構造化 |
| 21:00 | 就寝前 | AIが1週間サマリーを生成し、体調トレンドを確認 |
| 月1回 | ケアマネ面談 | PDFをダウンロードして持参、またはメール送付 |

---

### ペルソナ B: 山本 浩二（55歳）

- **職業:** 中小企業の営業マネージャー（管理職、裁量労働制）
- **介護状況:** 父（83歳）が脳梗塞後遺症で要介護2。父は独居、週2〜3回訪問して介護
- **家族構成:** 妻（フルタイム勤務）、子ども独立済み
- **困りごと:**
  - 遠隔介護のため父の日常状態の把握が難しく、訪問のたびに変化に驚く
  - 要介護度更新の申請や障害者手帳など、手続きが複雑で何から始めるかわからない
  - 訪問介護ヘルパーとの申し送りが口頭のみで抜け漏れが生じる
  - 介護休業・介護休暇の制度を活用できておらず有給を消耗している

**典型的な1日の利用シナリオ（浩二さん）**

| 時間 | 行動 | MimamoAI の活用 |
|------|------|-----------------|
| 土曜 10:00 | 父宅訪問 | 父の状態（足のむくみ写真 + テキストメモ）を記録入力 |
| 土曜 11:30 | ヘルパーとの申し送り | 記録から自動生成された引き継ぎサマリーをヘルパーに共有 |
| 日曜 | 手続き確認 | 「要介護度を上げたい。どうすればいい?」と手続きガイドに質問 |
| 日曜 | 書類作成 | ガイドの誘導でケアマネへの相談メモ・主治医意見書依頼レターを生成 |
| 月曜 朝 | 出社前 | 今月の介護実績をもとに会社提出用の介護休暇取得理由書を出力 |

---

## セクション2: 機能仕様

### P0-1: ケア記録の自動構造化

**概要:** 自然言語テキスト（および写真）を入力すると、AIが標準化されたケア日誌フォーマットに構造化する。

#### 入力
- 自由テキスト（例: 「今日は朝から食欲なくて、昼もほとんど食べなかった。3回トイレに行って、夕方に少し混乱があった」）
- オプション: 写真（傷・むくみ・食事の様子など。最大5枚）
- 記録日時（自動補完、手動修正可）
- 記録者（ログイン中のユーザー）
- 被介護者（登録済みから選択）

#### 処理（Claude API）
- **プロンプト方針:**
  - 入力テキストから以下の項目を抽出・補完: 食事（量・内容）、排泄（回数・性状）、睡眠、服薬、体調（痛み・発熱・むくみ等）、精神状態（穏やか・不安・混乱等）、転倒リスク、特記事項
  - 写真が添付されている場合: 画像の内容（傷の状態、食事の量感等）をテキスト補足として追加
  - 不明・未記載の項目は「未記録」とし、推測で埋めない
  - 出力はJSON構造で返す（フロントエンドが表示に利用）
  - トーン: 医療・介護の専門用語を適度に使用しつつ、家族介護者でも読みやすい表現

**プロンプトテンプレート（方針）:**
```
あなたは介護記録の構造化アシスタントです。
以下の自由記述から、標準的なケア日誌の各項目を抽出してください。
推測や補完はせず、記述から読み取れる情報のみを記録してください。

入力テキスト: {user_input}
添付画像の説明: {image_description_or_none}

以下のJSONフォーマットで出力してください:
{
  "date": "YYYY-MM-DD",
  "meal": { "breakfast": "", "lunch": "", "dinner": "", "hydration": "" },
  "elimination": { "urine_count": null, "stool": "" },
  "sleep": { "hours": null, "quality": "" },
  "medication": { "taken": null, "notes": "" },
  "physical": { "temperature": null, "pain": "", "edema": "", "other": "" },
  "mental": { "mood": "", "cognition": "", "behavior": "" },
  "fall_risk": "",
  "special_notes": "",
  "care_given": []
}
```

#### 出力
- 構造化されたケア日誌（編集可能なフォームとして表示）
- 確認・修正後に保存するボタン
- 保存後、記録一覧に追加

#### エッジケース
| ケース | 対応 |
|--------|------|
| 入力が極端に短い（例: 「普通」） | 「情報が少ないため詳細を確認してください」と表示し、追加入力を促すフォームを出す |
| 写真が鮮明でない・関係ない画像 | 画像からの情報は「判別不可」として記録し、テキスト情報のみで構造化 |
| 複数日分をまとめて入力 | 最新日として記録し、「複数日のデータが含まれている可能性があります。ご確認ください」と警告 |
| ネットワークエラー | 入力テキストをローカルストレージに一時保存し、再接続後に再送信 |

---

### P0-2: ケアマネ向け報告書PDF生成

**概要:** 一定期間のケア記録を集計・要約し、ケアマネジャーへの月次報告書をPDFで生成する。

#### 入力
- 報告期間（デフォルト: 前月1日〜末日、手動変更可）
- 被介護者情報（登録済みから自動取得）
- 報告書テンプレート選択（標準 / 簡易）
- 追記事項（任意の自由テキスト）

#### 処理（Claude API + PDF生成）
- **Step 1 (Claude API):** 指定期間のケア記録（JSON配列）を渡し、以下を生成
  - 期間サマリー（体調傾向・特記事項のハイライト）
  - 項目別集計テキスト（食事状況、排泄状況、精神状態の変化傾向）
  - 医療機関への申し送り候補（続く症状・異常値）
  - 今後のケアプランへの提案（オプション）
- **Step 2 (PDF生成):** サマリー + 集計データをHTMLテンプレートに流し込み、ブラウザの `window.print()` またはサーバーサイドの html-pdf ライブラリでPDF化

**プロンプトテンプレート（方針）:**
```
あなたは介護支援専門員（ケアマネジャー）への月次報告書作成アシスタントです。
以下の {N} 件のケア記録を読み、専門的かつ簡潔な月次報告書を作成してください。

被介護者: {care_recipient_name}（{age}歳、要介護{level}）
報告期間: {start_date} 〜 {end_date}
記録件数: {count}件

ケア記録データ:
{json_records}

以下のセクションを含むレポートを作成してください:
1. 総合所見（200字以内）
2. 体調・ADL（食事・排泄・睡眠・移動）の傾向
3. 認知・精神状態の変化
4. 特記事項・インシデント
5. 次月への申し送り事項
```

#### 出力
- プレビュー画面（HTMLレンダリング）
- 修正可能なテキストフィールド（各セクション）
- 「PDFダウンロード」ボタン
- 「コピー（テキスト）」ボタン

#### エッジケース
| ケース | 対応 |
|--------|------|
| 指定期間の記録が0件 | 「この期間に記録がありません」と表示し、期間変更を促す |
| 記録が3件以下で少ない | 「記録件数が少ないため、報告書の精度が低い可能性があります」と警告表示 |
| 被介護者情報が未登録 | 報告書生成前に被介護者登録を促すモーダルを表示 |
| PDF生成失敗 | テキスト形式でのコピー・ダウンロードにフォールバック |

---

### P1-3: 介護手続きガイド + 書類生成

**概要:** 対話形式で介護手続き（介護保険申請、要介護度更新、各種給付・手当等）をガイドし、必要書類のドラフトを生成する。

#### 入力
- 自由質問テキスト（例: 「父の介護度を上げるにはどうすればいいですか?」）
- 状況のクイック選択（任意: 「新規申請」「更新」「異議申し立て」「制度調査」）
- 都道府県・市区町村（手続き窓口情報の精度向上のため）

#### 処理（Claude API）
- **Step 1 (分類):** 質問を手続きカテゴリに分類
  - 介護保険関連（新規申請・更新・区分変更・異議申し立て）
  - 給付・助成関連（高額介護サービス費・住宅改修・福祉用具）
  - 職場制度関連（介護休業・介護休暇・所定外労働制限）
  - 医療連携関連（主治医意見書・医療費控除）
- **Step 2 (ガイド生成):** ステップバイステップの手続き説明を生成
  - 必要書類リスト
  - 申請窓口（市区町村の担当部署）
  - 所要期間の目安
  - よくある落とし穴
- **Step 3 (書類ドラフト):** ユーザーが「書類を作成する」を選択した場合、必要情報を収集し書類ドラフトを生成

**プロンプトテンプレート（方針）:**
```
あなたは介護手続きのエキスパートアシスタントです。
日本の介護保険制度・社会保障制度に精通しており、家族介護者が迷わず手続きを進められるよう支援します。

ユーザーの状況: {user_query}
都道府県: {prefecture}

以下の形式で回答してください:
1. 手続きの概要（何のための手続きか）
2. ステップ一覧（番号付き、各ステップ1〜2行）
3. 必要書類リスト
4. 申請窓口
5. 注意点・よくある失敗

不明な制度や地域固有の詳細は「お住まいの市区町村にご確認ください」と明記し、推測で回答しないこと。
```

#### 出力
- チャット形式のガイド表示
- 手続きステップのチェックリスト（完了マーク付き）
- 生成された書類ドラフト（編集可能テキストエリア）
- 書類のテキストコピーまたはPDFダウンロード

#### エッジケース
| ケース | 対応 |
|--------|------|
| 制度が地域によって異なる | 「詳細はお住まいの市区町村へご確認ください」を明記 |
| 法改正で情報が古い可能性 | 回答末尾に「情報は {knowledge_cutoff} 時点のものです。最新情報はご確認ください」を付記 |
| 医療・法律的判断が必要な質問 | 「専門家（ケアマネジャー・弁護士・医師）にご相談ください」と誘導 |
| 質問の意図が不明 | 状況を確認するクラリファイ質問を返す |

---

## セクション3: 画面構成

### 主要画面リスト

| # | 画面名 | パス | 説明 |
|---|--------|------|------|
| 1 | ランディング / ログイン | `/` | サービス紹介、ログイン・新規登録 |
| 2 | ホーム（ダッシュボード） | `/home` | 最近の記録サマリー、クイックアクション |
| 3 | 記録入力 | `/record/new` | ケア記録の新規作成（テキスト+写真） |
| 4 | 記録詳細・編集 | `/record/:id` | 個別記録の確認・編集 |
| 5 | 記録一覧 | `/records` | カレンダー/リスト形式で記録を一覧 |
| 6 | 報告書生成 | `/report/new` | 期間指定→プレビュー→PDFダウンロード |
| 7 | 報告書履歴 | `/reports` | 過去生成した報告書一覧 |
| 8 | 手続きガイド | `/guide` | 介護手続きチャットインターフェース |
| 9 | 被介護者管理 | `/care-recipients` | 被介護者の登録・編集 |
| 10 | アカウント設定 | `/settings` | プロフィール、通知設定 |

---

### 各画面の主要UI要素

#### 画面1: ランディング / ログイン
- ヒーローコピー（介護離職・ストレスへの共感メッセージ）
- 機能紹介カード（3つ）
- Googleログイン / メール+パスワード登録ボタン
- デモ動画または画面キャプチャ

#### 画面2: ホーム（ダッシュボード）
- ウェルカムバー（ユーザー名、今日の日付）
- 被介護者選択ドロップダウン（複数登録時）
- 今週のケア記録サマリーカード
  - 記録日数 / 未記録日数
  - 体調トレンド（3段階: 安定 / 注意 / 要確認）
- クイックアクション
  - 「今日の記録を入力する」（大ボタン）
  - 「報告書を生成する」
  - 「手続きを調べる」
- 直近5件の記録リスト（タイトル+日付）
- 次回ケアマネ面談まであと○日（登録時）

#### 画面3: 記録入力
- 被介護者選択（デフォルト: 直近選択）
- 日時ピッカー（デフォルト: 現在時刻）
- テキストエリア（自由記述、プレースホルダー例あり）
- 写真アップロードエリア（ドラッグ&ドロップ、最大5枚）
- 「AIで構造化する」ボタン
- 構造化結果プレビュー（項目別編集可能フォーム）
- 「保存する」「やり直す」ボタン
- ローディング状態（AIが生成中のスピナー）

#### 画面4: 記録詳細・編集
- 記録日時ヘッダー
- 構造化データの読み取り表示（各カテゴリカード形式）
- 添付写真サムネイル
- 「編集する」ボタン（インライン編集モード）
- 「削除する」ボタン（確認モーダル付き）
- 前後の記録へのナビゲーション矢印

#### 画面5: 記録一覧
- カレンダービュー（記録あり日に色付きドット）
- リストビュー切り替えボタン
- 月切り替えナビゲーション
- 記録カード（日付・体調サマリー1行・写真有無アイコン）
- 検索・フィルター（日付範囲、体調ラベル）
- 「新規記録」フローティングボタン

#### 画面6: 報告書生成
- ステップインジケーター（設定 → プレビュー → 完了）
- Step 1: 設定
  - 被介護者選択
  - 報告期間ピッカー（開始日・終了日）
  - テンプレート選択
  - 追記事項テキストエリア
  - 「報告書を生成する」ボタン
- Step 2: プレビュー
  - HTMLレンダリングの報告書
  - 各セクションのインライン編集
  - 「PDFダウンロード」「テキストコピー」ボタン
- Step 3: 完了
  - 保存確認・履歴へのリンク

#### 画面7: 報告書履歴
- 報告書カード一覧（期間・生成日・被介護者名）
- 「再ダウンロード」「削除」アクション
- 空状態表示（「まだ報告書がありません」）

#### 画面8: 手続きガイド
- チャットインターフェース（メッセージバブル形式）
- クイックスタートカード（「介護保険の申請方法」「要介護度を上げたい」「介護休業とは?」等）
- テキスト入力フォーム（送信ボタン）
- AIの回答内: 手続きステップチェックリスト
- 「書類を作成する」インラインボタン
- 書類ドラフト表示 + コピー / PDFボタン

#### 画面9: 被介護者管理
- 登録済み被介護者カード（名前・年齢・要介護度）
- 「新規追加」ボタン
- 被介護者フォーム（名前・生年月日・要介護度・主治医・担当ケアマネ名）

#### 画面10: アカウント設定
- プロフィール編集（名前・メール）
- 通知設定（記録リマインダー、アラート通知）
- ログアウト

---

### 画面遷移フロー

```
[ホーム]
    |-- [記録を入力] --> [記録入力] --> [記録詳細] --> [記録一覧]
    |                                       |
    |                                  (編集/削除)
    |
    |-- [報告書を生成] --> [報告書生成（Step1:設定）]
    |                           --> [Step2:プレビュー]
    |                               --> [Step3:完了] --> [報告書履歴]
    |
    |-- [手続きを調べる] --> [手続きガイド]
    |                           --> (書類生成インライン)
    |
    |-- [ヘッダー設定] --> [被介護者管理]
                       --> [アカウント設定]
```

---

## セクション4: データモデル

### エンティティ一覧

#### User（ユーザー）

| フィールド | 型 | 説明 |
|----------|-----|------|
| id | UUID | プライマリキー |
| email | string | ログイン用メール |
| name | string | 表示名 |
| prefecture | string | 都道府県（手続きガイド用） |
| created_at | timestamp | 登録日時 |
| updated_at | timestamp | 更新日時 |

---

#### CareRecipient（被介護者）

| フィールド | 型 | 説明 |
|----------|-----|------|
| id | UUID | プライマリキー |
| user_id | UUID | FK → User |
| name | string | 被介護者氏名 |
| birth_date | date | 生年月日 |
| gender | enum | male / female / other |
| care_level | enum | support1/2, care1〜5, none |
| primary_doctor | string | 主治医名（任意） |
| care_manager | string | 担当ケアマネ名（任意） |
| care_manager_contact | string | ケアマネ連絡先（任意） |
| notes | text | 特記事項（疾患・禁忌等） |
| created_at | timestamp | |
| updated_at | timestamp | |

---

#### CareRecord（ケア記録）

| フィールド | 型 | 説明 |
|----------|-----|------|
| id | UUID | プライマリキー |
| user_id | UUID | FK → User |
| care_recipient_id | UUID | FK → CareRecipient |
| record_date | date | 記録対象日 |
| record_time | time | 記録時刻（任意） |
| raw_input | text | ユーザーの自由記述テキスト |
| structured_data | JSONB | AI構造化結果（下記スキーマ参照） |
| photos | string[] | 写真URLの配列 |
| status | enum | draft / saved |
| created_at | timestamp | |
| updated_at | timestamp | |

`structured_data` JSONスキーマ:
```json
{
  "meal": {
    "breakfast": "string",
    "lunch": "string",
    "dinner": "string",
    "hydration": "string"
  },
  "elimination": {
    "urine_count": "number | null",
    "stool": "string"
  },
  "sleep": {
    "hours": "number | null",
    "quality": "string"
  },
  "medication": {
    "taken": "boolean | null",
    "notes": "string"
  },
  "physical": {
    "temperature": "number | null",
    "pain": "string",
    "edema": "string",
    "other": "string"
  },
  "mental": {
    "mood": "string",
    "cognition": "string",
    "behavior": "string"
  },
  "fall_risk": "string",
  "special_notes": "string",
  "care_given": ["string"]
}
```

---

#### Report（報告書）

| フィールド | 型 | 説明 |
|----------|-----|------|
| id | UUID | プライマリキー |
| user_id | UUID | FK → User |
| care_recipient_id | UUID | FK → CareRecipient |
| period_start | date | 報告期間開始 |
| period_end | date | 報告期間終了 |
| template_type | enum | standard / simple |
| generated_content | JSONB | セクション別生成テキスト（下記参照） |
| additional_notes | text | ユーザー追記事項 |
| pdf_url | string | 生成済みPDFのURL（任意） |
| record_count | integer | 集計対象の記録件数 |
| created_at | timestamp | |
| updated_at | timestamp | |

`generated_content` JSONスキーマ:
```json
{
  "overall_assessment": "string",
  "adl_summary": "string",
  "mental_summary": "string",
  "incidents": "string",
  "handover_notes": "string"
}
```

---

#### GuideSession（手続きガイドセッション）

| フィールド | 型 | 説明 |
|----------|-----|------|
| id | UUID | プライマリキー |
| user_id | UUID | FK → User |
| messages | JSONB | チャット履歴（role + content の配列） |
| category | string | 分類された手続きカテゴリ |
| generated_documents | JSONB[] | 生成された書類ドラフト一覧 |
| created_at | timestamp | |
| updated_at | timestamp | |

---

### リレーション図（テキスト表現）

```
User 1 ----< CareRecipient (1対多)
User 1 ----< CareRecord (1対多)
User 1 ----< Report (1対多)
User 1 ----< GuideSession (1対多)
CareRecipient 1 ----< CareRecord (1対多)
CareRecipient 1 ----< Report (1対多)
```

---

## セクション5: APIフロー

### 5.1 Claude API 連携

#### A. ケア記録構造化（P0-1）

```
[フロントエンド]
     |
     | POST /api/records/structure
     | body: { raw_text, images_base64[], care_recipient_id }
     |
     v
[バックエンド (Next.js API Route / Lovable)]
     |
     |-- 画像が添付されている場合:
     |   1. 各画像を base64 → Claude Vision に渡し、画像の説明テキストを生成
     |      (claude-3-5-sonnet, messages API, vision対応)
     |   2. 画像説明テキストを raw_text に付記
     |
     |-- Claude API 呼び出し (claude-3-5-haiku or sonnet)
     |   messages: [
     |     { role: "system", content: <構造化システムプロンプト> },
     |     { role: "user", content: raw_text + image_descriptions }
     |   ]
     |   response_format: JSON object
     |
     v
[バックエンド]
     |-- JSONレスポンスをパース・バリデーション
     |-- CareRecord テーブルに status=draft で保存
     |
     v
[フロントエンド]
     |-- 構造化結果をフォームに展開・表示
     |-- ユーザーが確認・修正 → 「保存」 → PATCH /api/records/:id { status: "saved" }
```

**Claude API パラメータ:**
- Model: `claude-3-5-haiku-20241022`（コスト最適化）
- Max tokens: 1000
- Temperature: 0（確定的な構造化）

---

#### B. 報告書生成（P0-2）

```
[フロントエンド]
     |
     | POST /api/reports/generate
     | body: { care_recipient_id, period_start, period_end, template_type, additional_notes }
     |
     v
[バックエンド]
     |
     |-- DB から期間内の CareRecord を全件取得 (structured_data のみ)
     |-- レコード数チェック (0件→エラー, 3件以下→警告フラグ)
     |
     |-- Claude API 呼び出し (claude-3-5-sonnet)
     |   messages: [
     |     { role: "system", content: <報告書生成システムプロンプト> },
     |     { role: "user", content: JSON.stringify(records) + additional_notes }
     |   ]
     |   Max tokens: 2000
     |
     |-- 生成テキストを JSON セクションにパース
     |-- Report テーブルに保存
     |
     v
[フロントエンド]
     |-- 報告書プレビュー表示 (HTML テンプレートにデータ注入)
     |-- PDF生成: ブラウザの window.print() を印刷用CSSで制御
     |   または: html2canvas + jsPDF ライブラリ
```

**Claude API パラメータ:**
- Model: `claude-3-5-sonnet-20241022`（品質重視）
- Max tokens: 2000
- Temperature: 0.2（一定の表現バリエーション）

---

#### C. 手続きガイド（P1-3）

```
[フロントエンド]
     |
     | POST /api/guide/message
     | body: { session_id (nullable), user_message, prefecture }
     |
     v
[バックエンド]
     |
     |-- session_id が null → 新規 GuideSession 作成
     |-- 既存 session → messages 履歴を取得
     |
     |-- Claude API 呼び出し (claude-3-5-sonnet, streaming)
     |   messages: [
     |     { role: "system", content: <手続きガイドシステムプロンプト> },
     |     ...conversation_history,
     |     { role: "user", content: user_message }
     |   ]
     |   stream: true
     |
     |-- ストリーミングレスポンスをフロントエンドに SSE で転送
     |-- 完了後: messages 履歴を GuideSession に保存
     |
     v
[フロントエンド]
     |-- ストリーミングテキストをチャットバブルに逐次表示
     |-- 「書類を作成」ボタンが含まれる場合: 書類生成フローへ
```

**Claude API パラメータ:**
- Model: `claude-3-5-sonnet-20241022`
- Max tokens: 1500
- Temperature: 0.3
- Stream: true

---

### 5.2 n8n ワークフロー設計

#### ワークフロー 1: 記録リマインダー通知

```
[トリガー] Cron: 毎日 20:00 JST
    |
    v
[Webhook / DB Query] 本日の記録が0件のユーザーを取得
    |
    v
[条件分岐] 記録なし?
    |-- Yes --> [メール送信 / ブラウザPush通知]
    |           「今日の介護記録がまだ未入力です」
    |-- No  --> 終了
```

#### ワークフロー 2: 月次報告書自動リマインダー

```
[トリガー] Cron: 毎月25日 9:00 JST
    |
    v
[DB Query] 当月分の報告書未生成ユーザー一覧を取得
    |
    v
[ループ] 各ユーザーに対して:
    |-- [メール送信]
    |   件名: 「来月のケアマネ報告書の作成時期です」
    |   本文: 報告書生成画面へのリンク
```

#### ワークフロー 3: 異常パターン検知アラート（P2）

```
[トリガー] Webhook: 新規 CareRecord 保存時
    |
    v
[データ取得] 直近7日間の同ユーザー・同被介護者の記録を取得
    |
    v
[Claude API 呼び出し]
    入力: 直近7件の structured_data
    プロンプト: 「以下の記録から、医療的注意が必要な異常パターンを検出してください。
                 正常範囲内の場合は null を返してください」
    |
    v
[条件分岐] 異常検知あり?
    |-- Yes --> [プッシュ通知 + メール]
    |           「{被介護者名}さんの状態に注意が必要なパターンが検出されました」
    |-- No  --> 終了
```

---

### 5.3 PDF生成の方法

**採用方式: ブラウザ側 `window.print()` + 印刷専用CSS（Lovable との相性が最も良い）**

```
手順:
1. 報告書データを HTML テンプレートコンポーネントに渡してレンダリング
2. 印刷専用 CSS (`@media print`) で:
   - ヘッダー/フッター/ナビゲーションを非表示
   - A4サイズにフォーマット
   - ページ区切りの制御 (page-break-before/after)
3. JavaScript: window.print() 呼び出し → ブラウザのPDF保存ダイアログ

代替案（サーバーサイド、時間があれば）:
- html-pdf または Puppeteer (サーバーレス環境では制約あり)
- Alibaba Cloud Function Compute で Puppeteer を実行
```

**PDFレイアウト仕様（A4縦）:**
```
┌─────────────────────────────┐
│ MimamoAI                     │  ← ロゴ・サービス名
│ ケアマネジャー向け月次報告書    │
├─────────────────────────────┤
│ 被介護者: 〇〇 様             │
│ 報告期間: 〇月〇日〜〇月〇日   │
│ 作成者: △△                   │
│ 作成日: 〇〇〇〇年〇月〇日     │
├─────────────────────────────┤
│ 1. 総合所見                   │
│ （テキスト）                  │
├─────────────────────────────┤
│ 2. 体調・ADLの傾向            │
│ （テキスト）                  │
├─────────────────────────────┤
│ 3. 認知・精神状態              │
│ 4. 特記事項                   │
│ 5. 次月申し送り事項            │
└─────────────────────────────┘
```

---

### 5.4 写真のハンドリング

```
アップロードフロー:
1. フロントエンド: <input type="file" accept="image/*"> で最大5ファイル選択
2. クライアント側リサイズ: Canvas API で長辺1024px以下にリサイズ（Claude API 入力コスト削減）
3. バックエンド:
   a. Lovable の組み込みストレージ または Alibaba Cloud OSS にアップロード
   b. 返却された URL を CareRecord.photos 配列に保存
4. Claude Vision 呼び出し時:
   - 保存済みURLから画像を取得 または base64 を直接渡す
   - image/jpeg で送信（png より軽量）

セキュリティ:
- 署名付きURL（有効期限付き）でアクセス制御
- ファイルタイプ検証（MIME type: image/jpeg, image/png, image/webp のみ）
- 最大ファイルサイズ: 5MB/枚（リサイズ後）
```

---

*以上、セクション1〜5 完了。*
*作成日: 2026-03-07*
